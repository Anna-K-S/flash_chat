import 'package:flutter/material.dart';

class TextStyles {
  static const sendButton = TextStyle(
    color: Colors.lightBlueAccent,
    fontWeight: FontWeight.bold,
    fontSize: 18.0,
  );

  static const message = TextStyle(
    color: Colors.white,
    fontSize: 15.0,
  );

  static const sender = TextStyle(
    fontSize: 15.0,
    color: Colors.black54,
  );

  static const subtitle = TextStyle(
    color: Color.fromARGB(255, 85, 80, 80),
    fontSize: 18.0,
    fontWeight: FontWeight.w400,
  );

  static const title = TextStyle(
    color: Colors.black,
    fontSize: 30.0,
    fontWeight: FontWeight.bold,
  );

  static const forgotPasswordButton = TextStyle(
    fontWeight: FontWeight.bold,
    fontSize: 12.0,
    color: Colors.blueAccent,
  );
}
